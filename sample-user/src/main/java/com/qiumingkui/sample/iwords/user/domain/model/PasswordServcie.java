package com.qiumingkui.sample.iwords.user.domain.model;

public class PasswordServcie {
	
	/**
	 * 口令加密
	 * 
	 * @param originalText
	 *            口令原文
	 * @return 口令密文
	 */
	public static String encryptPassword(String originalText) {
		// 加密过程...
		String cipherText = originalText;
		return cipherText;
	}

}
